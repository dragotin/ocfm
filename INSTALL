
To compile ownCloud File Manager, install Qt5 devel packages and a C++ compiler.

To build ocfm, run the following command:

```
   mkdir build
   cd build
   cmake ..
   make
```

Install in your system as user root:

   make install

See cmake tutorials for more options.

